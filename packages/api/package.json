{
  "name": "loyalty-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "node --test test/**/*.test.js",
    "test:integration": "node --test integration/api-integration.test.mjs",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.131",
    "@types/node": "20.10.0",
    "typescript": "~5.3.0"
  }
}
